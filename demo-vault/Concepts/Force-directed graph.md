---
tags: [concepts]
aliases: [Force-directed graph]
---

# Force-directed graph

Notes are nodes. `[[wikilinks]]` are edges. The layout is a small force simulation that runs in the browser — pan, pinch-zoom, tap to select, drag to rearrange.
