---
tags: [concepts]
aliases: [Self-contained HTML]
---

# Self-contained HTML

One Python script + one template → one HTML file. No CDN, no build step, no network. You can AirDrop it to a phone and still open notes offline.

See also [[Force-directed graph]] and [[Wikilinks]].
